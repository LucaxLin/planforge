# PlanForge Backend Environment Variables
# Copy this file to .env and fill in your values

# Server Configuration
PORT=3001
NODE_ENV=production

# CORS Configuration - Only allow your frontend domain
CORS_ORIGIN=https://planforge.lucaslinn.cc.cd

# AI API Configuration (User will configure via frontend)
# These are defaults, users will set their own via the frontend UI
